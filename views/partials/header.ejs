<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Foodies</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display'] bg-gray-50 text-gray-800">
    <nav class="w-full px-8 py-4 bg-white shadow-md flex items-center justify-between">
        <div>
            <h1 class="text-5xl text-orange-500 font-bold tracking-wider">Foodies</h1>
        </div>
        <% var loggedIn = typeof loggedIn !== 'undefined' ? loggedIn : true %>
        <% if (loggedIn) { %>
            <div class="flex gap-6 items-center">
                <a href="/shop" class="text-xl text-gray-700 hover:text-orange-500 transition duration-200 font-medium">Shop</a>
                <a href="/cart" class="text-xl text-gray-700 hover:text-orange-500 transition duration-200 font-medium">Cart</a>
                <a href="/" class="text-xl text-gray-700 hover:text-orange-500 transition duration-200 font-medium">My Account</a>
                <a href="/users/logout" class="text-xl text-red-500 font-medium hover:text-red-700 transition duration-200">Logout</a>
            </div>
        <% } %>
    </nav>
</body>
</html>
